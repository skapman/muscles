# 🎨 Режимы Визуализации Графа

## Вопрос: Как выглядит "общий граф"?

Отличный вопрос! Есть несколько подходов к визуализации всего графа знаний.

---

## 🌐 Вариант 1: Полный Граф (All Nodes)

**Концепция:** Показать ВСЕ узлы одновременно со всеми связями.

### Как выглядит (пример с 50 узлами):

```
        Цель-1 ←→ Упр-1 ←→ Мышца-1
          ↓         ↓         ↓
        Цель-2 ←→ Упр-2 ←→ Мышца-2
          ↓         ↓         ↓
        Боль-1 ←→ Упр-3 ←→ Мышца-3
          ↓         ↓         ↓
        Цель-3 ←→ Упр-4 ←→ Мышца-4
          ...      ...       ...
```

**С D3.js force-directed:**
- Узлы автоматически расталкиваются
- Связанные узлы притягиваются друг к другу
- Образуются естественные кластеры

### Плюсы:
- ✅ Видно всю картину сразу
- ✅ Можно найти неожиданные связи
- ✅ Красиво выглядит (как на empatika.com)

### Минусы:
- ❌ При >50 узлах становится хаотично
- ❌ Сложно найти конкретный узел
- ❌ Нужен zoom/pan для навигации
- ❌ Медленно работает при >200 узлах

### Пример реализации:

```javascript
// Показать ВСЕ узлы
const allNodes = [
  ...goals,      // 10 целей
  ...exercises,  // 30 упражнений
  ...muscles,    // 20 мышц
  ...pain        // 5 болей
]; // Итого: 65 узлов

const allEdges = [
  // Все связи между всеми узлами
  { from: 'goal:bench-100kg', to: 'exercise:bench-press' },
  { from: 'exercise:bench-press', to: 'muscle:pectoralis-major' },
  { from: 'pain:lower-back-pain', to: 'muscle:erector-spinae' },
  // ... ещё ~150 связей
];

graph.render({ nodes: allNodes, edges: allEdges });
```

---

## 🎯 Вариант 2: Контекстный Граф (Focused View)

**Концепция:** Показать один узел + его окружение (1-2 уровня связей).

### Как выглядит:

```
Пользователь кликает на "Жать 100кг"
↓
Показываем только связанные узлы:

        [ЦЕЛЬ: Жать 100кг]
               ↓
    ┌──────────┼──────────┐
    ↓          ↓          ↓
[Жим лёжа] [Жим на наклонной] [Жим узким хватом]
    ↓          ↓          ↓
[Грудные]  [Дельты]  [Трицепс]
```

### Плюсы:
- ✅ Чисто и понятно
- ✅ Фокус на релевантной информации
- ✅ Быстро работает
- ✅ Легко навигировать

### Минусы:
- ❌ Не видно всей картины
- ❌ Нужно кликать для исследования

---

## 🔀 Вариант 3: Гибридный (Рекомендуемый)

**Концепция:** Комбинация обоих подходов с фильтрами.

### Интерфейс:

```
┌─────────────────────────────────────────────────┐
│  Граф Знаний                          [Фильтры] │
├─────────────────────────────────────────────────┤
│                                                 │
│  Режим: ○ Полный граф  ● Фокус  ○ Кластеры    │
│                                                 │
│  Показать: ☑ Цели  ☑ Упражнения  ☑ Мышцы      │
│            ☑ Боли  ☐ Системы                   │
│                                                 │
│  Детализация: [━━━━━●━━━━] 1-3 уровня связей   │
│               ↑ Ползунок глубины графа          │
│                                                 │
│  ┌───────────────────────────────────────────┐ │
│  │                                           │ │
│  │         [Визуализация графа]              │ │
│  │                                           │ │
│  │  • Zoom/Pan                               │ │
│  │  • Клик на узел → детали                 │ │
│  │  • Двойной клик → фокус на узле           │ │
│  │                                           │ │
│  └───────────────────────────────────────────┘ │
│                                                 │
│  Легенда:                                       │
│  🎯 Цели  💪 Мышцы  🏋️ Упражнения  ⚠️ Боли     │
└─────────────────────────────────────────────────┘
```

### Режимы:

#### 1. **Полный граф** (Full Graph)
- Показать все узлы
- Автоматическая группировка по типам
- Цветовое кодирование

#### 2. **Фокус** (Focused View)
- Выбрать центральный узел
- Показать только связанные узлы (1-2 уровня)
- Остальные узлы затемнены или скрыты

#### 3. **Кластеры** (Clusters)
- Группировка по типам (цели, упражнения, мышцы)
- Каждый кластер - отдельная группа
- Связи между кластерами

---

## 🎨 Визуальные Примеры

### Полный Граф (с фильтрами)

**Все типы узлов:**
```
     🎯        🎯        🎯
   Цель-1    Цель-2    Цель-3
     ↓         ↓         ↓
   🏋️       🏋️       🏋️
  Упр-1    Упр-2    Упр-3
     ↓         ↓         ↓
   💪       💪       💪
 Мышца-1  Мышца-2  Мышца-3
     ↓         ↓         ↓
   ⚠️       ⚠️       ⚠️
  Боль-1   Боль-2   Боль-3
```

**Только Цели + Упражнения (фильтр):**
```
     🎯        🎯        🎯
   Цель-1    Цель-2    Цель-3
     ↓         ↓         ↓
   🏋️       🏋️       🏋️
  Упр-1    Упр-2    Упр-3
```

### Фокус на Узле

**Клик на "Жать 100кг":**
```
Затемнённые узлы (не связаны):
  Цель-2 (dim)
  Цель-3 (dim)
  Упр-4 (dim)

Яркие узлы (связаны):
        [ЦЕЛЬ: Жать 100кг] ← ЦЕНТР
               ↓
    ┌──────────┼──────────┐
    ↓          ↓          ↓
[Жим лёжа] [Жим на наклонной] [Жим узким]
    ↓          ↓          ↓
[Грудные]  [Дельты]  [Трицепс]
```

### Кластеры

```
┌─────────────┐   ┌─────────────┐   ┌─────────────┐
│   ЦЕЛИ      │   │ УПРАЖНЕНИЯ  │   │   МЫШЦЫ     │
│             │   │             │   │             │
│  • Цель-1   │──→│  • Упр-1    │──→│  • Мышца-1  │
│  • Цель-2   │──→│  • Упр-2    │──→│  • Мышца-2  │
│  • Цель-3   │──→│  • Упр-3    │──→│  • Мышца-3  │
│             │   │             │   │             │
└─────────────┘   └─────────────┘   └─────────────┘
       ↓                 ↓                 ↓
┌─────────────┐   ┌─────────────┐   ┌─────────────┐
│    БОЛИ     │   │   СИСТЕМЫ   │   │   ГАДЖЕТЫ   │
└─────────────┘   └─────────────┘   └─────────────┘
```

---

## 🚀 Моя Рекомендация: Гибридный Подход

### Фаза 1: Фокусный Режим (MVP)
**Почему:**
- Проще реализовать
- Понятнее пользователю
- Быстрее работает

**Как работает:**
1. Пользователь выбирает узел (цель, упражнение, мышцу)
2. Показываем этот узел + связанные (1-2 уровня)
3. Клик на связанный узел → переход к нему

**Пример:**
```javascript
// Пользователь кликает на "Жать 100кг"
const graph = DataResolver.buildRelationshipGraph('goals', 'bench-100kg', 2);
// Возвращает: центральный узел + упражнения + мышцы (2 уровня)

relationshipGraph.show(graph);
```

### Фаза 2: Полный Граф с Фильтрами
**Добавляем:**
- Кнопка "Показать полный граф"
- Фильтры по типам узлов
- Поиск узлов
- Zoom/Pan

**Пример:**
```javascript
// Показать все узлы с фильтрами
const filters = {
  showGoals: true,
  showExercises: true,
  showMuscles: true,
  showPain: false,
  showSystems: false
};

const fullGraph = DataResolver.buildFullGraph(filters);
relationshipGraph.showFull(fullGraph);
```

### Фаза 3: Кластерный Режим
**Добавляем:**
- Группировка по типам
- Сворачивание/разворачивание кластеров
- Связи между кластерами

---

## 🎚️ Ползунок Детализации (Depth Slider)

**Концепция:** Как на empatika.com - регулировать уровень детализации графа.

### Как работает:

```
Детализация: [━━━━━●━━━━] 1-3 уровня
             ↑
             Текущий уровень: 2

Уровень 1: Только прямые связи
  [ЦЕЛЬ] → [Упр-1] [Упр-2] [Упр-3]

Уровень 2: + Связи второго уровня
  [ЦЕЛЬ] → [Упр-1] → [Мышца-1]
        → [Упр-2] → [Мышца-2]
        → [Упр-3] → [Мышца-3]

Уровень 3: + Связи третьего уровня
  [ЦЕЛЬ] → [Упр-1] → [Мышца-1] → [Боль-1]
        → [Упр-2] → [Мышца-2] → [Боль-2]
```

### Реализация:

```javascript
// js/ui/relationshipGraph.js

export class RelationshipGraph {
  constructor(containerId) {
    this.container = document.getElementById(containerId);
    this.currentDepth = 2; // По умолчанию 2 уровня
    this.maxDepth = 3;
    this.currentNode = null;
  }

  createDepthSlider() {
    const sliderContainer = document.createElement('div');
    sliderContainer.className = 'depth-slider-container';

    const label = document.createElement('label');
    label.textContent = 'Детализация: ';

    const slider = document.createElement('input');
    slider.type = 'range';
    slider.min = '1';
    slider.max = this.maxDepth.toString();
    slider.value = this.currentDepth.toString();
    slider.className = 'depth-slider';

    const valueDisplay = document.createElement('span');
    valueDisplay.className = 'depth-value';
    valueDisplay.textContent = `${this.currentDepth} ${this.getLevelText(this.currentDepth)}`;

    slider.addEventListener('input', (e) => {
      this.currentDepth = parseInt(e.target.value);
      valueDisplay.textContent = `${this.currentDepth} ${this.getLevelText(this.currentDepth)}`;

      // Перестроить граф с новой глубиной
      if (this.currentNode) {
        this.updateGraph(this.currentNode.type, this.currentNode.id, this.currentDepth);
      }
    });

    sliderContainer.appendChild(label);
    sliderContainer.appendChild(slider);
    sliderContainer.appendChild(valueDisplay);

    return sliderContainer;
  }

  getLevelText(depth) {
    const texts = {
      1: 'уровень связей',
      2: 'уровня связей',
      3: 'уровня связей'
    };
    return texts[depth] || 'уровней связей';
  }

  updateGraph(entityType, entityId, depth) {
    // Получить новый граф с обновлённой глубиной
    const graph = DataResolver.buildRelationshipGraph(entityType, entityId, depth);

    // Плавно обновить визуализацию
    this.transitionToNewGraph(graph);
  }

  transitionToNewGraph(newGraph) {
    // D3.js transition для плавного добавления/удаления узлов
    // При увеличении глубины - добавляем новые узлы
    // При уменьшении - удаляем дальние узлы

    // Подробная реализация в коде
  }
}
```

### Визуальный пример:

```
┌─────────────────────────────────────────┐
│ Детализация: [━━━━━●━━━━] 2 уровня     │
│              1    2    3                │
│                                         │
│ Уровень 1: Только упражнения            │
│ Уровень 2: + Мышцы                      │
│ Уровень 3: + Боли и связанные цели      │
└─────────────────────────────────────────┘
```

### Поведение при изменении:

**Увеличение глубины (1 → 2):**
- Новые узлы появляются с анимацией fade-in
- Новые связи рисуются с анимацией
- Force simulation пересчитывает позиции

**Уменьшение глубины (3 → 2):**
- Дальние узлы исчезают с fade-out
- Связи удаляются
- Оставшиеся узлы перегруппировываются

### Оптимизация производительности:

```javascript
// Кэширование графов разной глубины
class GraphCache {
  constructor() {
    this.cache = new Map();
  }

  getGraph(entityType, entityId, depth) {
    const key = `${entityType}:${entityId}:${depth}`;

    if (!this.cache.has(key)) {
      const graph = DataResolver.buildRelationshipGraph(entityType, entityId, depth);
      this.cache.set(key, graph);
    }

    return this.cache.get(key);
  }

  clear() {
    this.cache.clear();
  }
}
```

---

## 💡 Интерактивность

### Базовая (Фаза 1):
- **Клик на узел** → показать детали в боковой панели
- **Двойной клик** → перефокусировать граф на этом узле
- **Hover** → подсветить связанные узлы
- **Ползунок детализации** → изменить глубину графа (1-3 уровня)

### Продвинутая (Фаза 2):
- **Drag узла** → переместить (D3.js force simulation)
- **Zoom/Pan** → навигация по большому графу
- **Поиск** → найти и подсветить узел
- **Фильтры** → показать/скрыть типы узлов
- **Ползунок + фильтры** → комбинированная фильтрация

### Экспертная (Фаза 3):
- **Shortest path** → найти кратчайший путь между двумя узлами
- **Clustering** → автоматическая группировка похожих узлов
- **Timeline** → показать эволюцию графа (если добавляем версионность)
- **Динамическая глубина** → автоматическая подстройка под размер экрана

---

## 📊 Примеры Реальных Данных

### Малый граф (10-20 узлов)
**Сценарий:** Фокус на "Жать 100кг"
```
Узлы:
- 1 цель (Жать 100кг)
- 5 упражнений (жимы)
- 3 мышцы (грудные, дельты, трицепс)
- 1 боль (импинджмент плеча)

Связи:
- Цель → Упражнения (5 связей)
- Упражнения → Мышцы (15 связей)
- Боль → Мышцы (3 связи)

Итого: 10 узлов, 23 связи
```

### Средний граф (50-100 узлов)
**Сценарий:** Все спортивные цели
```
Узлы:
- 10 целей
- 30 упражнений
- 20 мышц
- 5 болей

Связи: ~150

Итого: 65 узлов, 150 связей
```

### Большой граф (200+ узлов)
**Сценарий:** Весь граф знаний
```
Узлы:
- 20 целей
- 50 упражнений
- 30 мышц
- 10 болей
- 20 системных блоков
- 10 гаджетов

Связи: ~400

Итого: 140 узлов, 400 связей
```

---

## ✅ Итоговое Решение

### Начинаем с Фокусного Режима:
1. **Пользователь выбирает узел** (цель, упражнение, мышцу)
2. **Показываем граф вокруг этого узла** (1-2 уровня связей)
3. **Интерактивность:** клик → детали, двойной клик → перефокус

### Затем добавляем Полный Граф:
1. **Кнопка "Показать полный граф"**
2. **Фильтры по типам узлов**
3. **Zoom/Pan для навигации**
4. **Поиск узлов**

### Визуализация:
- **D3.js force-directed layout** для органичного расположения
- **Цветовое кодирование** по типам узлов
- **Размер узлов** зависит от количества связей
- **Толщина связей** зависит от "силы" связи
- **Ползунок детализации** для контроля глубины графа (1-3 уровня)

### Контролы:
- **Ползунок детализации** - регулировать количество уровней связей
- **Фильтры типов** - показать/скрыть категории узлов
- **Поиск** - найти конкретный узел
- **Zoom/Pan** - навигация по графу

---

## 🎯 Приоритет Реализации Ползунка

### Фаза 1 (MVP):
- ✅ Базовый ползунок (1-3 уровня)
- ✅ Перестроение графа при изменении
- ✅ Отображение текущего уровня

### Фаза 2:
- ✅ Плавные анимации при изменении глубины
- ✅ Кэширование графов для производительности
- ✅ Комбинация с фильтрами

### Фаза 3:
- ✅ Автоматическая подстройка под размер экрана
- ✅ Адаптивная глубина (больше узлов = меньше уровней)
- ✅ Сохранение настроек в localStorage

---

**Готовы начинать реализацию с ползунком детализации!** 🚀
