# 🎛️ Настройка Физики Графа

## Параметры Плавности Движения

При создании графа можно настроить параметры физики D3.js для контроля плавности движения узлов:

```javascript
const graph = new RelationshipGraph('graph-container', {
    width: window.innerWidth,
    height: window.innerHeight - 100,
    enableDrag: true,

    // Параметры физики
    velocityDecay: 0.4,    // Плавность движения (0-1)
    alphaTarget: 0.3,      // Интенсивность при drag (0-1)
    alphaDecay: 0.02       // Скорость затухания (0-1)
});
```

---

## 📊 Параметры

### 1. `velocityDecay` (по умолчанию: 0.4)
**Что делает:** Контролирует, насколько быстро узлы замедляются

**Диапазон:** 0 - 1
- **0** = узлы никогда не останавливаются (хаос)
- **0.2** = очень плавное, медленное движение
- **0.4** = сбалансированная плавность ✅ (по умолчанию)
- **0.6** = быстрое затухание
- **1** = мгновенная остановка (нет инерции)

**Когда менять:**
- Увеличить (0.5-0.7) → если узлы слишком долго "болтаются"
- Уменьшить (0.2-0.3) → если хотите более плавное, "жидкое" движение

---

### 2. `alphaTarget` (по умолчанию: 0.3)
**Что делает:** Насколько "горячей" становится симуляция при перетаскивании узла

**Диапазон:** 0 - 1
- **0** = симуляция не активируется (узлы не реагируют)
- **0.1** = слабая реакция других узлов
- **0.3** = средняя реакция ✅ (по умолчанию)
- **0.5** = сильная реакция (узлы активно двигаются)
- **1** = максимальная активность (может быть хаотично)

**Когда менять:**
- Увеличить (0.4-0.6) → если хотите, чтобы другие узлы сильнее реагировали на drag
- Уменьшить (0.1-0.2) → если хотите более спокойное поведение

---

### 3. `alphaDecay` (по умолчанию: 0.02)
**Что делает:** Как быстро симуляция "остывает" после действия

**Диапазон:** 0 - 1
- **0.001** = очень медленное затухание (долгая анимация)
- **0.01** = медленное затухание
- **0.02** = среднее затухание ✅ (по умолчанию)
- **0.05** = быстрое затухание
- **0.1** = очень быстрое затухание (короткая анимация)

**Когда менять:**
- Уменьшить (0.01) → если хотите, чтобы узлы дольше двигались после drag
- Увеличить (0.05-0.1) → если хотите быструю стабилизацию

---

## 🎨 Примеры Настроек

### Очень Плавное, "Жидкое" Движение
```javascript
{
    velocityDecay: 0.2,    // Медленное затухание скорости
    alphaTarget: 0.4,      // Сильная реакция
    alphaDecay: 0.01       // Долгая анимация
}
```
**Эффект:** Узлы двигаются плавно и долго, как в воде

---

### Быстрое, Отзывчивое Движение
```javascript
{
    velocityDecay: 0.6,    // Быстрое затухание скорости
    alphaTarget: 0.2,      // Слабая реакция
    alphaDecay: 0.05       // Быстрая стабилизация
}
```
**Эффект:** Узлы быстро реагируют и быстро останавливаются

---

### Сбалансированное (По умолчанию)
```javascript
{
    velocityDecay: 0.4,    // Средняя плавность
    alphaTarget: 0.3,      // Средняя реакция
    alphaDecay: 0.02       // Среднее затухание
}
```
**Эффект:** Хороший баланс между плавностью и отзывчивостью ✅

---

### Эффект "Empatika.com"
```javascript
{
    velocityDecay: 0.3,    // Плавное движение
    alphaTarget: 0.35,     // Хорошая реакция
    alphaDecay: 0.015      // Долгое затухание
}
```
**Эффект:** Похоже на граф empatika - узлы долго "шевелятся"

---

## 🔧 Как Применить

### В graph-demo.html:
```javascript
const graph = new RelationshipGraph('graph-container', {
    width: window.innerWidth,
    height: window.innerHeight - 100,
    defaultDepth: 2,
    maxDepth: 3,
    enableDrag: true,

    // Добавьте эти параметры
    velocityDecay: 0.3,
    alphaTarget: 0.35,
    alphaDecay: 0.015
});
```

---

## 📝 Дополнительные Параметры D3.js

Если нужен еще более тонкий контроль, можно настроить силы напрямую в `renderCanvasGraph()`:

```javascript
this.simulation = d3.forceSimulation(this.graphData.nodes)
    .force('link', d3.forceLink(validEdges)
        .id(d => d.id)
        .distance(100)           // Расстояние между связанными узлами
        .strength(1))            // Сила связей (0-1)
    .force('charge', d3.forceManyBody()
        .strength(-300))         // Сила отталкивания (отрицательная)
    .force('center', d3.forceCenter(width / 2, height / 2)
        .strength(0.05))         // Сила притяжения к центру
    .force('collide', d3.forceCollide()
        .radius(30)              // Радиус коллизии
        .strength(0.7))          // Сила коллизии
    .velocityDecay(0.4)
    .alphaDecay(0.02);
```

---

## 🎯 Рекомендации

1. **Начните с дефолтных значений** (0.4, 0.3, 0.02)
2. **Меняйте по одному параметру** за раз
3. **Тестируйте на мобильных** - там эффект более заметен
4. **Для "волшебного эффекта"** как на empatika:
   - Уменьшите `velocityDecay` (0.2-0.3)
   - Уменьшите `alphaDecay` (0.01-0.015)
   - Увеличьте `alphaTarget` (0.35-0.4)

---

## ✅ Текущие Настройки

**По умолчанию в коде:**
```javascript
velocityDecay: 0.4
alphaTarget: 0.3
alphaDecay: 0.02
```

**Толщина связей:** 2px (было 1px)

Эти значения дают хороший баланс между плавностью и производительностью! 🚀
